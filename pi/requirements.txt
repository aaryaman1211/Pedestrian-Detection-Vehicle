opencv-python-headless>=4.8.0
ultralytics>=8.0.0
numpy>=1.24.0
pyserial>=3.5

# Pi Camera (install on Raspberry Pi OS):
#   sudo apt install -y python3-picamera2
# GPIO for ultrasonic (install on Pi):
#   pip install RPi.GPIO
